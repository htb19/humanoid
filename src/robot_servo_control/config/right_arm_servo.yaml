use_gazebo: false

## Properties of incoming commands
command_in_type: "speed_units"
scale:
  linear: 0.4
  rotational: 0.8
  joint: 0.5

## Properties of outgoing commands
publish_period: 0.01
low_latency_mode: true

command_out_type: trajectory_msgs/JointTrajectory

publish_joint_positions: true
publish_joint_velocities: true
publish_joint_accelerations: false

## Smoothing
smoothing_filter_plugin_name: "online_signal_smoothing::ButterworthFilterPlugin"

## Planning Scene
# MoveGroup 负责主 planning scene，两个 Servo 节点都设为 false
is_primary_planning_scene_monitor: false

## MoveIt properties
move_group_name: right_arm
planning_frame: base_link

## Frames
ee_frame_name: right_wrist_yaw_link
# robot_link_command_frame: base_link
robot_link_command_frame: base_link

## Stopping behaviour
incoming_command_timeout: 0.2
num_outgoing_halt_msgs_to_publish: 4

## Singularity & joint limits
lower_singularity_threshold: 17.0
hard_stop_singularity_threshold: 30.0
joint_limit_margin: 0.3
leaving_singularity_threshold_multiplier: 2.0

## Topic names
# 运行时被 namespace "right_servo" 修饰
# 最终为 /right_servo/delta_twist_cmds 等
cartesian_command_in_topic: ~/delta_twist_cmds
joint_command_in_topic: ~/delta_joint_cmds
joint_topic: /joint_states
status_topic: ~/status
command_out_topic: /right_arm_controller/joint_trajectory

## Collision checking
check_collisions: true
collision_check_rate: 5.0
self_collision_proximity_threshold: 0.01
scene_collision_proximity_threshold: 0.02